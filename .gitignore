# Shared ignores. App-specific rules live in frontend/.gitignore and backend/.gitignore.

# env files (examples are committed)
.env*
!.env.example

# misc
.DS_Store
*.pem
.vscode/
.idea/

# Reference PDFs the question bank was built from (too large for git; the JSON bank is committed)
data/*.pdf
